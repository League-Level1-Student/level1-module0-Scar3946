void setup () {
background (20) ;
setSize(500,500);
PImage ;                        //as member variable
        size(600,600);                                 //in setup method
        pictureOfRecord= loadImage("record.png");      //in setup method
        
}
void draw(){
image(, xPosition, yPosition);  //in draw method  
}
